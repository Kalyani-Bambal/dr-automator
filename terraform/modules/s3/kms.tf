#############################################################
# KMS Alias lookup is not required for bucket operations.
# The bucket uses the KMS key directly via the key ARN.
#############################################################

